create react project using vite 
npm create vite@latest 
package name : client 
install tailwind :
run this: npm install tailwindcss @tailwindcss/vite
add this in vite.config.ts: 

import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'

import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react(),tailwindcss()]

})

this in index.css : @import "tailwindcss";

delete app.css 
add assets ,components, context folders in src folder 
and paste inside assets folder 
install react router dom ,
react-testify,
quill
download assets from here : https://greatstack.dev/assets/job-portal-react-js
create 3 new files : ApplyJob.jsx , Home.jsx , Applications.jsx inside pages folder 
now create react router : 
import { BrowserRouter } from 'react-router-dom'
createRoot(document.getElementById('root')).render(
  <BrowserRouter>
    <App />
  </BrowserRouter>,
)
now import route and routes from react-router-dom into app.jsx
<Routes>
      <Route path='/' element ={<Home/>}/>
      <Route path='/apply-job/:id' element ={<ApplyJob/>}/>
      <Route path='/application' element ={<Applications/>}/>

    </Routes>
    paste this code : 
here after : the colon (:) before id means that id is a route parameter (a dynamic value that changes for each request).
create a new file : AppContext.jsx inside context folder 
and paste this code : 
import { createContext } from "react";

export const AppContext = createContext();

export const AppContextProvider = (props) =>{
  const value = {

  }
  return (<AppContext.Provider value = {value}>
    {props.children}
  </AppContext.Provider>)
}

Note: use rafce shortcut to create basic template of react pages


